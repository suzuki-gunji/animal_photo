<%= @nickname %>さんのプロフィール
<p><%= @user.profile %></p>
<% if @user.image.present? %>
  <p><%= image_tag @user.image.thumb.url %></p>
<% end %>

<% if current_user.id == @user.id? %>
  <p><%= link_to 'プロフィール修正', edit_user_path(@user.id) %></p>
<% end %>

<%= @nickname %>さんの投稿一覧

<% @posts.each do |post| %>
  <p><%= image_tag post.image.thumb.url, class: 'post-image' %></p>
  <p>タイトル : <%= post.title %></p>
<% end %>

<%= link_to 'もどる', root_path, class:"back-btn" %>